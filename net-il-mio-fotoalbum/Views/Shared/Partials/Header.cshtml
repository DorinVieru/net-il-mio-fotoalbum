<nav class="navbar navbar-expand-lg navbar-dark box-shadow">
    <div class="container-fluid py-3">
        <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
            <img class="img-fluid me-5" src="~/img/logo-photo2.png" alt="Logo" style="max-width: 170px;" />
        </a>
        <button class="navbar-toggler btn-warning" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link text-white fw-bold" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                @if (User.IsInRole("SuperAdmin") || User.IsInRole("User") || User.IsInRole("Photographer"))
                {
                    <li class="nav-item">
                        <a class="nav-link text-white fw-bold" asp-area="" asp-controller="Photo" asp-action="Index">Admin Foto</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white fw-bold" asp-area="" asp-controller="Category" asp-action="Index">Categorie</a>
                    </li>
                }
                @if (User.IsInRole("SuperAdmin") || User.IsInRole("Photographer"))
                {
                    <li class="nav-item">
                        <a class="nav-link text-white fw-bold" asp-area="" asp-controller="Message" asp-action="Index">Messaggi</a>
                    </li>
                }
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <!-- code omitted -->
                <partial name="_LoginPartial" />
            </ul>
        </div>
    </div>
</nav>
